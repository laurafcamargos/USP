#include <stdio.h>
int main()
{
    int N, i;

    scanf("%d", &N);

    int vetor[N];

    for (i = 0; i < N; i++)
    {
        scanf("%d", &vetor[i]);
    }
    for (i = N - 1; i >= 0; i--)
    {

        printf("%d ", vetor[i]);
    }

    return 0;
}
